export enum Difficulty {
    EASY = "EASY",
    MEDIUM = "MEDIUM",
    HARD = "HARD"
}

export enum SortOrder {
    asc,
    desc
}

export enum SortBy {
    questionId = 1,
    title,
    difficulty
}